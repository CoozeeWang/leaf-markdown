# 变更记录

这里记录 Leaf 每个对外版本的可见变化。版本号与 `package.json` 保持一致。

## [未发布]

### 新增

- 拖入或插入 HEIC／HEIF（iPhone 照片）和 SVG 时按图片处理，能显示的平台直接显示
- 图片显示不出来时正文里给出占位块，说明原因、文件名和下一步；点击可在文件管理器中找到该文件

### 修复

- 标题多级编号与折叠箭头不再重叠：编号打开时箭头固定住在标题左侧预留的空位里，序号跟在箭头右侧，几位数都不会压上去
- 鼠标悬停标题折叠箭头不再出现椭圆底色，箭头与标题文字之间留出稳定间距（编辑视图和阅读视图都生效）
- 有序列表增删一项后，后面的编号自动连续：删除中间项不再留下 1、3、4，列表开头那一项被删时整张列表沿用原来的起始号
- 导出 PDF 不再被「当前平台不支持的格式」卡住：这类图片以占位文字导出，文件丢失或损坏仍会拦住导出

## [0.4.0-beta.2] — 2026-09-23

### 新增

- 「显示」下拉：把文档属性、正文上方的文件名、源码行号、编辑标记、标题多级编号集中到一处开关板
- 设置面板底部常驻显示版本号与构建标识

### 变更

- 顶栏重新分组：保存与导出从文件名菜单里拆出来，移到设置控件左侧、挨着放
- 保存改用软盘图标，导出改用托盘加向上箭头
- 文档属性开关从文件名菜单移到工具栏
- 设置面板分为「外观与排版」和「保存与恢复」两个标签页

### 修复

- 顶栏按钮组不再压住文件名标题
- 关闭对话框的「放弃」按钮恢复边框
- 去掉工具栏里重复的标题多级编号按钮
- 关闭最后一个文档窗口后直接退出，不再弹回欢迎页
- 保存时确认覆盖同名文件，正确替换已有文档，而不是另存一份
- 保存纯文本不再扫描文档所在目录，大目录下不再停顿
- 打开文档时的保存提示从顶部移到状态栏
- 文档附件（图片等）在重命名、另存为、导出后仍正确指向
- 输入时图片预览与行号不再跳动
- 段落间距统一，段首多余空行自动清理
- 属性块可用回车确认，空属性块自动移除
- 窗口控件居中对齐，并限制最小窗口宽度
- Windows 上创建文档窗口不再占用界面线程，打开文档不再卡顿

## [0.4.0-beta.1] — 2026-09-22

首个对外测试版号。内容与内部 0.3.70 测试包相同，改动只涉及打包与测试配置，功能见下方 0.3.x 条目。

## [0.3.x] — 2026-09-08 ~ 2026-09-22

发布前的内部迭代，共五十余个版本号（0.3.14 → 0.3.70），未对外发布。期间完成的主要能力：

- 从网页原型迁移到 macOS 桌面端，确立一窗口一文档、启动器窗口打开后隐藏的多窗口模型
- 结构化 Markdown 实时预览、阅读模式、纸质打印样式
- 脚注、列表、表格的编辑与整理
- YAML 属性编辑与类型控件
- 保存历史与可恢复草稿，支持按周期过期清理
- 标注（callout）图标与整套界面规范
- 建立跨平台测试基线
